{
  "name": "clerk-demo-store",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:verify": "npm run build && npm run validate",
    "preview": "astro preview",
    "validate": "node scripts/validate-storefront.mjs",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.3.1"
  },
  "allowScripts": {
    "esbuild": true
  }
}
